name: kolbo-desktop-whitelabel-release

on:
  workflow_dispatch:
    inputs:
      whitelabel:
        description: "Whitelabel slug (e.g. sapir)"
        required: true
        default: "sapir"
      version:
        description: "Version to release (e.g. 1.0.0)"
        required: true
        default: "1.0.0"
      draft:
        description: "Create as draft release"
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.inputs.whitelabel }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  WHITELABEL: ${{ github.event.inputs.whitelabel }}
  KOLBO_VERSION: ${{ github.event.inputs.version }}

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            rust-target: x86_64-pc-windows-msvc
            bundle: nsis
            artifact-suffix: windows-x64
          - platform: macos-latest
            rust-target: aarch64-apple-darwin
            bundle: dmg
            artifact-suffix: darwin-aarch64

    runs-on: ${{ matrix.platform }}
    if: endsWith(github.repository, '/kolbo-code')

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: packages/desktop/src-tauri

      - name: Install dependencies
        run: bun install

      - name: Set version
        working-directory: packages/desktop
        run: |
          node -e "
            const pkg = require('./package.json');
            pkg.version = '${{ env.KOLBO_VERSION }}';
            require('fs').writeFileSync('./package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
          echo "Version set to ${{ env.KOLBO_VERSION }}"

      - name: Fix symlinks (Windows)
        if: matrix.platform == 'windows-latest'
        run: |
          cp packages/ui/src/custom-elements.d.ts packages/app/src/custom-elements.d.ts

      - name: Read whitelabel config
        id: wl
        shell: bash
        run: |
          node -e "
            const c = require('./packages/desktop/whitelabels/${{ env.WHITELABEL }}/config.json');
            const fs = require('fs');
            const env = process.env.GITHUB_OUTPUT;
            fs.appendFileSync(env, 'product_name=' + c.productName + '\n');
            if (c.brandName) fs.appendFileSync(env, 'brand_name=' + c.brandName + '\n');
            if (c.apiBase) fs.appendFileSync(env, 'api_base=' + c.apiBase + '\n');
            if (c.appBase) fs.appendFileSync(env, 'app_base=' + c.appBase + '\n');
            if (c.logoImage) fs.appendFileSync(env, 'logo_image=' + c.logoImage + '\n');
          "

      - name: Apply whitelabel branding
        shell: bash
        run: |
          cd packages/desktop
          WHITELABEL="${{ env.WHITELABEL }}"
          node -e "
            const fs = require('fs');
            const conf = JSON.parse(fs.readFileSync('src-tauri/tauri.conf.json', 'utf8'));
            const wl = JSON.parse(fs.readFileSync('whitelabels/${WHITELABEL}/config.json', 'utf8'));
            conf.productName = wl.productName;
            conf.identifier = wl.identifier;
            conf.mainBinaryName = wl.mainBinaryName;
            conf.bundle.icon = [
              'icons/${WHITELABEL}/32x32.png',
              'icons/${WHITELABEL}/128x128.png',
              'icons/${WHITELABEL}/128x128@2x.png',
              'icons/${WHITELABEL}/icon.icns',
              'icons/${WHITELABEL}/icon.ico',
            ];
            if (!conf.bundle.windows) conf.bundle.windows = {};
            if (!conf.bundle.windows.nsis) conf.bundle.windows.nsis = {};
            conf.bundle.windows.nsis.installerIcon = 'icons/${WHITELABEL}/icon.ico';
            conf.bundle.windows.nsis.headerImage = 'assets/${WHITELABEL}-nsis-header.bmp';
            conf.bundle.windows.nsis.sidebarImage = 'assets/${WHITELABEL}-nsis-sidebar.bmp';
            if (conf.plugins?.['deep-link']?.desktop) {
              conf.plugins['deep-link'].desktop.schemes = [wl.deepLinkScheme];
            }
            if (!conf.plugins) conf.plugins = {};
            if (!conf.plugins.updater) conf.plugins.updater = {};
            conf.plugins.updater.endpoints = [
              'https://github.com/Zoharvan12/kolbo-code/releases/download/updater/${WHITELABEL}-latest.json'
            ];
            fs.writeFileSync('src-tauri/tauri.conf.json', JSON.stringify(conf, null, 2) + '\n');
          "
          # Fix TypeScript globals
          node -e "
            const fs = require('fs');
            const files = ['src/index.tsx', 'src/updater.ts', 'src-tauri/src/windows.rs'];
            for (const f of files) {
              try {
                let c = fs.readFileSync(f, 'utf8');
                c = c.replace(/__OPENCODE__/g, '__KOLBO_CODE__').replace(/\.title\(\"OpenCode\"\)/g, '.title(\"Kolbo Code\")');
                fs.writeFileSync(f, c);
              } catch {}
            }
          "

      - name: Download CLI sidecar
        shell: bash
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          mkdir -p packages/desktop/src-tauri/sidecars
          mkdir -p /tmp/kolbo-cli-install
          cd /tmp/kolbo-cli-install
          npm install @kolbo/kolbo-code@latest 2>&1 || true
          cd "$GITHUB_WORKSPACE"
          BASE="/tmp/kolbo-cli-install/node_modules"
          PLATFORM="${{ matrix.platform }}"
          RUST_TARGET="${{ matrix.rust-target }}"
          if [ "$PLATFORM" == "windows-latest" ]; then
            KOLBO_BIN="$BASE/@kolbo/kolbo-code/node_modules/@kolbo/kolbo-code-windows-x64/bin/kolbo.exe"
            [ -f "$KOLBO_BIN" ] || KOLBO_BIN="$BASE/@kolbo/kolbo-code-windows-x64/bin/kolbo.exe"
            DEST="packages/desktop/src-tauri/sidecars/opencode-cli-${RUST_TARGET}.exe"
          elif [ "$PLATFORM" == "macos-latest" ]; then
            KOLBO_BIN="$BASE/@kolbo/kolbo-code/node_modules/@kolbo/kolbo-code-darwin-arm64/bin/kolbo"
            [ -f "$KOLBO_BIN" ] || KOLBO_BIN="$BASE/@kolbo/kolbo-code-darwin-arm64/bin/kolbo"
            DEST="packages/desktop/src-tauri/sidecars/opencode-cli-${RUST_TARGET}"
          else
            KOLBO_BIN="$BASE/@kolbo/kolbo-code/node_modules/@kolbo/kolbo-code-darwin-x64/bin/kolbo"
            [ -f "$KOLBO_BIN" ] || KOLBO_BIN="$BASE/@kolbo/kolbo-code-darwin-x64/bin/kolbo"
            DEST="packages/desktop/src-tauri/sidecars/opencode-cli-${RUST_TARGET}"
          fi
          if [ -f "$KOLBO_BIN" ]; then
            cp "$KOLBO_BIN" "$DEST"
            echo "✓ CLI sidecar copied from $KOLBO_BIN"
          else
            echo "⚠ Binary not found, listing @kolbo packages:"
            ls "$BASE/@kolbo/" 2>/dev/null || echo "None found"
          fi

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          VITE_WHITELABEL_SPLASH: /whitelabels/${{ env.WHITELABEL }}/splash.jpg
          VITE_WHITELABEL_LOGO: ${{ steps.wl.outputs.logo_image }}
          VITE_WHITELABEL_NAME: ${{ steps.wl.outputs.brand_name }}
          KOLBO_WHITELABEL_API_BASE: ${{ steps.wl.outputs.api_base }}
          KOLBO_WHITELABEL_APP_BASE: ${{ steps.wl.outputs.app_base }}
        with:
          projectPath: packages/desktop
          tauriScript: bunx tauri
          args: --target ${{ matrix.rust-target }} --bundles ${{ matrix.bundle }}
          tagName: ${{ env.WHITELABEL }}-desktop-v${{ env.KOLBO_VERSION }}
          releaseName: "${{ steps.wl.outputs.product_name }} v${{ env.KOLBO_VERSION }}"
          releaseBody: |
            ## ${{ steps.wl.outputs.product_name }} v${{ env.KOLBO_VERSION }}

            AI coding agent desktop app — whitelabel build.

            ### Download
            - **Windows**: Setup installer (.exe)
            - **macOS**: Disk image (.dmg)
          releaseDraft: ${{ github.event.inputs.draft }}
          prerelease: false

      - name: Upload update bundles to release (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $tag = "${{ env.WHITELABEL }}-desktop-v${{ env.KOLBO_VERSION }}"
          $dir = Join-Path $env:GITHUB_WORKSPACE "packages\desktop\src-tauri\target\x86_64-pc-windows-msvc\release\bundle\nsis"
          Write-Host "Looking in: $dir"
          $zip = Get-ChildItem $dir -Filter "*.nsis.zip" | Select-Object -First 1
          if ($zip) {
            gh release upload $tag $zip.FullName "$($zip.FullName).sig" --clobber -R ${{ github.repository }}
            Write-Host "✓ Uploaded: $($zip.Name)"
          } else {
            Write-Host "⚠ No .nsis.zip found — contents:"
            Get-ChildItem $dir
          }

      - name: Upload update bundles to release (macOS)
        if: matrix.platform == 'macos-latest'
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ env.WHITELABEL }}-desktop-v${{ env.KOLBO_VERSION }}"
          DIR="packages/desktop/src-tauri/target/aarch64-apple-darwin/release/bundle/macos"
          TAR=$(find "${DIR}" -name "*.app.tar.gz" | head -1)
          if [ -f "${TAR}" ]; then
            gh release upload "${TAG}" "${TAR}" "${TAR}.sig" --clobber -R ${{ github.repository }}
            echo "✓ Uploaded: $(basename ${TAR})"
          else
            echo "⚠ No .app.tar.gz found in ${DIR}"
            ls "${DIR}" || true
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.WHITELABEL }}-desktop-${{ matrix.artifact-suffix }}
          path: |
            packages/desktop/src-tauri/target/${{ matrix.rust-target }}/release/bundle/**/*.exe
            packages/desktop/src-tauri/target/${{ matrix.rust-target }}/release/bundle/**/*.dmg
            packages/desktop/src-tauri/target/${{ matrix.rust-target }}/release/bundle/**/*.deb
          retention-days: 30

  publish-updater:
    needs: build
    runs-on: ubuntu-latest
    if: endsWith(github.repository, '/kolbo-code')
    steps:
      - name: Build and publish update manifest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WHITELABEL: ${{ github.event.inputs.whitelabel }}
          KOLBO_VERSION: ${{ github.event.inputs.version }}
        run: |
          TAG="${WHITELABEL}-desktop-v${KOLBO_VERSION}"
          REPO="${{ github.repository }}"

          mkdir -p /tmp/assets
          gh release download "${TAG}" -R "${REPO}" -D /tmp/assets/
          ls /tmp/assets/

          WIN_SIG_FILE=$(find /tmp/assets -name "*.nsis.zip.sig" | head -1)
          MAC_SIG_FILE=$(find /tmp/assets -name "*.app.tar.gz.sig" | head -1)

          if [ -z "${WIN_SIG_FILE}" ] || [ -z "${MAC_SIG_FILE}" ]; then
            echo "⚠ Missing signature files — Windows: '${WIN_SIG_FILE}' Mac: '${MAC_SIG_FILE}'"
            exit 1
          fi

          WIN_SIG=$(cat "${WIN_SIG_FILE}")
          MAC_SIG=$(cat "${MAC_SIG_FILE}")
          WIN_ZIP=$(basename "${WIN_SIG_FILE}" .sig)
          MAC_TAR=$(basename "${MAC_SIG_FILE}" .sig)
          WIN_URL="https://github.com/${REPO}/releases/download/${TAG}/${WIN_ZIP}"
          MAC_URL="https://github.com/${REPO}/releases/download/${TAG}/${MAC_TAR}"
          PUB_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          cat > ${WHITELABEL}-latest.json << MANIFEST
          {
            "version": "${KOLBO_VERSION}",
            "notes": "",
            "pub_date": "${PUB_DATE}",
            "platforms": {
              "windows-x86_64": {
                "signature": "${WIN_SIG}",
                "url": "${WIN_URL}"
              },
              "darwin-aarch64": {
                "signature": "${MAC_SIG}",
                "url": "${MAC_URL}"
              }
            }
          }
          MANIFEST

          gh release view updater -R "${REPO}" 2>/dev/null || \
            gh release create updater -R "${REPO}" \
              --title "Auto-updater manifest" \
              --notes "Auto-updated by CI. Do not delete this release." \
              --prerelease

          gh release upload updater ${WHITELABEL}-latest.json --clobber -R "${REPO}"
          echo "✓ Update manifest published as ${WHITELABEL}-latest.json"
